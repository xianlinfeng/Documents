0. cmake scip in Debug and Release model
cmake .. -DSOPLEX_DIR=~/Downloads/scipoptsuite-6.0.1/soplex/build -DCMAKE_INSTALL_PREFIX=~/scip_installation/Release -DCMAKE_BUILD_TYPE=Debug
cmake .. -DSOPLEX_DIR=~/Downloads/scipoptsuite-6.0.1/soplex/build -DCMAKE_INSTALL_PREFIX=~/scip_installation/Release

1. cmake Code in Debug model
cmake -DCMAKE_PREFIX_PATH=~/scip_installation/Debug/ -DCMAKE_BUILD_TYPE=Debug ..
Debug the code:  gdb --args ./reoptimization ../build/cats1658.lp 3

2. cmake Code in  Release model
cmake -DCMAKE_PREFIX_PATH=~/scip_installation/Release/ .. 




3. memory management
    a) Block Memory --> for array 
            nparams = SCIPgetNParams(scip);
            SCIP_CALL( SCIPallocBlockMemoryArray(scip, &array, nparams) );
            /*  do something */ 
            SCIPfreeBlockMemoryArray(scip, &array, nparams);
    
    b) Buffer Memory --> use within a function
        SCIPallocBuffer(), SCIPallocBufferArray()   to allocate memory,
        SCIPfreeBuffer(), SCIPfreeBufferArray()     to free memory.
        --> clearn buffer memory
        SCIPallocCleanBufferArray（）分配内存，
        SCIPfreeCleanBufferArray（）释放内存。

    c) increase memory allocate
        SCIPcalcMemGrowSize（）

4. Pointer
    a)  int var = 13;
        int* a = &var; 
        &var  // 访问var的内存地址
        a   // 表示内存地址
        *a   // 访问a的实际值

Questions:
3. after create and add variables y into problem, should I use y, or read y from variables.
4. what is the different between objective and coefficient? and LHS, RHS.
5. the Pointer and array in C need allocate memory before use them? 


